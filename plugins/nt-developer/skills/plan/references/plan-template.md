# <タイトル> 実装計画（<チケットID>）

作業ログ・実装メモ・WIP 用のローカルファイル（コミット対象外）。

<!-- 置き場所は plans/進行中/<管理表>/<タイトル>-<チケットID>.md -->
<!-- チケット化していない作業は plans/進行中/other/<タイトル>.md に置き、見出しの（<チケットID>）を削って代わりに以下を1行追加する -->
<!-- チケット化はしない（ユーザー判断） -->
<!-- 中身が無い節は見出しごと消せ。「なし」「特になし」と書くな -->
<!-- この md はエディタで直接開かれるため <details> は使うな（HTML/JSX混在ファイルと誤認され Markdown プレビューが効かなくなる）。折りたたまず最初から見せる部分（冒頭の要約 / 実装ステップ）を先頭にまとめ、それ以外は下の「詳細（承認時に読まなくてよい）」の下に置け -->

<何をするかの1〜2行の要約>

## 実装ステップ

- [ ] Step 1: <やること>
- [ ] Step 2: <やること>

<!-- ステップの切り方は plan skill の「実装ステップの粒度」節に従え（意味のある対応単位でまとめる／目安5ステップ以内／ファイル単位に割らない） -->
<!-- 実装が終わったら見出しを「対応内容（実施済み）」に変え、各項目を過去形でまとめ直す -->
<!-- タグ作成・GitHub Release作成・デプロイ等、リポジトリで既に固定の運用手順として文書化されている作業をここに書くな。PRマージ後に別セッションで実行されることが多く、チェックリストとして追跡すると更新漏れの温床になる。固定手順は各リポジトリの運用ドキュメント（README等）に一本化しろ -->

## 詳細（承認時に読まなくてよい）

### 確認テスト

<!-- plan skill の「確認テスト」の観点のうち該当するものだけを断定文にして書け。該当なしと判定した観点も理由つきで残せ。食い違いがゼロになるまで「確認テスト: 合格」と書くな -->
<!-- 該当した観点の断定文は、1件ずつ AskUserQuestion で提示した文をそのまま書け（hook が計画の断定文と実際に投げた質問文を突き合わせる） -->

該当した観点:

- <観点の名前>: <具体的な中身を埋めた断定文> — 確認済み（<YYYY-MM-DD>）

該当なしと判定した観点:

- <観点の名前>: <該当しないと判定した理由>

確認テスト: 合格（<YYYY-MM-DD>）

### 計画書レビュー

<!-- plan-review skill のレビュー観点を全部当てた結果を書け。見つけて直した項目が1件も無ければ「見つけて直した項目: なし」と書き、記録ごと省くな -->

見つけて直した項目:

- <何が抜けていたか>。<どう直したか>

直さなかった項目:

- <何を見つけたか>。直さない理由: <理由>

計画書レビュー: 合格（<YYYY-MM-DD>）

### 決めたこと

| 何を決めるか | 選べた案 | 決めた案 |
|---|---|---|
| <何を決めるか> | <A案> / <B案> | <決めた案。未確定なら「未確定」> |

#### 承認記録

- <対象範囲>: <何を承認したか> — ユーザー承認済み（<YYYY-MM-DD>）

### 参照

- **チケット（<管理表>）**: [<チケットID> <タイトル>](<URL>)
- **よりどころにする資料**: <Google Sheets / GitHub Issue 等、判断のよりどころにするドキュメント>
- **参考資料（経緯を追うだけで判断の根拠にしない）**: <議論の経緯を追うためだけの補助資料>

### 背景

<なぜこの対応が必要になったか>

### 現状

<該当ファイル・行番号つきで、今の実装がどうなっているかを確認した記録>

### 変更後・対応内容

<何をどう変えるか。対応表・コード片も貼ってよい>

<!-- 仕様解釈を書く行には引用元の原文を「<どのシート／チケット>の<どこ>に、こう書いてある: <原文>」の形で必ず貼れ（plan skill の「一次ソースの原文を引用しろ」）。要約で書くな -->
<!-- よりどころにする資料に明記が無く、この場で判断した項目は見出しに (よりどころにする資料に無いため今回決定) と明示する -->
<!-- よりどころにする資料に明記された期待値と違う挙動を採る場合・一部の対象にだけ与えられた根拠を他の対象へ広げる場合は、対象ごとにユーザーの承認を取ってから書け（plan skill の「よりどころにする資料から外れる判断・根拠の範囲外への適用」） -->
<!-- 「ユーザー承認済み」「合意済み」「決定」と書けるのは、会話でユーザー本人が明示的に承認を返した項目だけだ。チケットのコメント・仕様書の記述を根拠にした判断へこの表現を使うな -->
<!-- 承認記録は対象範囲ごとに1行で残す: - <対象（チケットID・画面名）>: <何を承認したか> — ユーザー承認済み（<YYYY-MM-DD>） -->
<!-- 比較検討・理由等の技術的な詳細は見出し＋箇条書きで書け -->

### 検証・完了条件

<テスト実行結果・動作確認の記録。数字が出せるものは数字で書く（例: 736 passed, 2 skipped）>

<!-- マージ後でないと確認できない項目（例: デプロイ後に実際にログが流れているか）は - [ ] のチェックボックスで書け。この未完タスクが PR マージ後も残っていると、deploy-watch skill がデプロイの完了を待ってから消化する -->
<!-- チケットの期待結果と採る実装が食い違う場合は、チケット側の期待結果の更新依頼も - [ ] のチェックボックスで書け -->

<!-- よりどころにする資料に明記された期待値と違う挙動を採る対象があるときは、この節を採用した挙動だけで書き換えるな。元の期待値を消さず、次の表を置け（plan skill の「逸脱の記録は「検証・完了条件」に残せ」）。逸脱が1件も無ければこの見出しと表ごと書くな -->
<!--
#### よりどころにする資料からの逸脱

| 対象 | よりどころにする資料の期待値（出所） | 採用した挙動 | 根拠の出所と適用範囲 | 承認 |
|---|---|---|---|---|
-->

### 採らなかった案

<検討したうえで選ばなかった案と、捨てた理由。何も比較せず一択で決めた場合はこの見出し自体を削ってよい>

- 案B: <内容>。捨てた理由: <理由>
- 何もしない場合: <放置したときに起きること>

### この判断を覆す条件

<何が起きたらこの方針を撤回・変更するか。恒久的に正しいと言い切れる変更（誤字修正等）では見出し自体を削ってよい>

- <条件>が起きたら、<どう変えるか>

### 触るファイル

- [ ] <path/to/file>
- [ ] <path/to/file>

### 未解決事項

<まだ答えが出ていないこと・判断を保留にしたこと。何も無ければこの見出し自体を削ってよい>

### 今回やらないこと

<対応しないと決めたこと。何も無ければこの見出し自体を削ってよい>
