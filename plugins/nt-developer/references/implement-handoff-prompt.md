`orca worktree create --prompt` に渡す引き継ぎ文の雛形。`__ISSUE_URL__` を実装対象の Issue の URL に、`__EXTRA__` を「本体のセッションで決まっていて Issue の description に書かれていない判断」に置き換えて渡せ。書き戻せるものは description へ書き戻し、`__EXTRA__` は `なし` にしろ。

ワークツリー側は新しいセッションだ。**ここに書かなかったことは一切引き継がれない。** 1行目の skill 起動を落とすと、ワークツリー側はチェックボックスの更新・計画への書き戻し・コミットしない運用を知らないまま実装する。

---

まず Skill ツールで `nt-developer:implement` を起動しろ。対象は __ISSUE_URL__ だ。実装計画は Issue の description に全部書いてある。

- **お前は Orca が用意したワークツリーの中で動いている。** 作業ツリーは既にある。自分で `orca worktree create` も `git worktree add` も叩くな。今いるディレクトリでそのまま作業しろ
- ブランチも既に切られている。`gh issue develop` を叩くな
- 実装着手は既に承認されている。実装ステップを最後まで進めろ
- 判断が割れるときだけ止まって聞け。逐一の確認・進捗報告で止まるな
- リポジトリ側の検査（テスト・skill の検査・バージョンの上げ忘れ）で止まったら、迂回せずに中身を直せ

本体のセッションで決まっていて Issue の description に書かれていない判断: __EXTRA__
